# 🎙️ GolosAI-DE - AI Voice Dialogue System for German Language

Интерактивная система для практики немецкого языка с использованием AI. Проект позволяет вести голосовые диалоги в различных сценариях (ресторан, знакомство, аэропорт) с автоматическим распознаванием речи и синтезом ответов.

## 🌟 Возможности

- 🎤 **Голосовой ввод** - запись через микрофон или загрузка аудио файлов
- 🗣️ **Распознавание речи** - автоматическая транскрипция немецкой речи (Whisper)
- 🤖 **AI диалоги** - контекстные ответы на основе выбранного сценария
- 🔊 **Синтез речи** - озвучивание ответов AI голосом (Piper TTS)
- 📝 **История диалогов** - сохранение и экспорт стенограмм разговоров
- 🎭 **Различные сценарии** - ресторан, знакомство, аэропорт

## 🏗️ Архитектура

Проект построен на микросервисной архитектуре с использованием Docker:

```
┌─────────────┐
│  Frontend   │ (Streamlit, port 3000)
│  (Streamlit)│
└──────┬──────┘
       │
┌──────▼──────┐
│   Backend   │ (FastAPI, port 8000)
│  (FastAPI)  │
└──┬──┬──┬────┘
   │  │  │
   │  │  └─────────┐
   │  │            │
┌──▼──▼───┐  ┌────▼────┐  ┌─────────┐
│   ASR   │  │   LLM   │  │   TTS   │
│(Whisper)│  │ (LLaMA) │  │ (Piper) │
│port 9001│  │port 9002│  │port 9003│
└─────────┘  └─────────┘  └─────────┘
```

## 📋 Требования

- Docker >= 20.10
- Docker Compose >= 2.0
- Минимум 8GB RAM
- 10GB свободного места на диске

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/YOUR_USERNAME/golosai-de.git
cd golosai-de
```

### 2. Создание необходимых директорий

```bash
mkdir -p models/asr models/llm models/tts
```

### 3. Скачивание моделей

#### TTS модели (обязательно)
```bash
cd models/tts

# Скачиваем голос Thorsten (мужской, высокое качество)
wget https://huggingface.co/rhasspy/piper-voices/resolve/main/de/de_DE/thorsten/high/de_DE-thorsten-high.onnx
wget https://huggingface.co/rhasspy/piper-voices/resolve/main/de/de_DE/thorsten/high/de_DE-thorsten-high.onnx.json

cd ../..
```

#### ASR модели (опционально - загружаются автоматически)
Модель Whisper загрузится автоматически при первом запуске ASR сервиса.

#### LLM модели (требуется настройка)
Поместите вашу LLM модель (например, LLaMA) в `models/llm/`

### 4. Запуск проекта

```bash
# Запуск всех сервисов
docker-compose up --build -d

# Просмотр логов
docker-compose logs -f

# Проверка статуса сервисов
docker-compose ps
```

### 5. Открытие приложения

Откройте браузер и перейдите по адресу:
```
http://localhost:3000
```

## 🎯 Использование

### Запись с микрофона

1. Выберите сценарий диалога
2. Перейдите на вкладку "🎤 Запись голосом"
3. Нажмите на кнопку записи
4. Говорите на немецком языке
5. Остановите запись
6. Нажмите "📤 Отправить запись"
7. Дождитесь ответа AI с озвучкой

### Загрузка файла

1. Перейдите на вкладку "📂 Загрузка файла"
2. Выберите WAV-файл с записью
3. Нажмите "Отправить файл"

### Завершение диалога

1. Нажмите "🏁 Завершить диалог"
2. Скачайте стенограмму разговора

## ⚙️ Конфигурация

### Изменение сценариев

Отредактируйте файл `config/scenarios.yaml`:

```yaml
scenarios:
  your_scenario:
    title: "Ваш сценарий"
    system_prompt: "Инструкция для AI ассистента"
```

### Настройка параметров

Отредактируйте файл `config/config.yaml`:

```yaml
asr:
  model: "large-v3"  # base, small, medium, large-v3
  language: "de"

llm:
  max_history: 8  # Количество сообщений в контексте

tts:
  default_voice: "de_DE-thorsten-high"

app:
  default_scenario: "restaurant"
  response_timeout: 30
```

## 🐛 Решение проблем

### Сервисы не запускаются

```bash
# Проверьте логи
docker-compose logs

# Пересоберите контейнеры
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### Ошибка "Model not found"

```bash
# Проверьте наличие моделей
ls -la models/tts/
ls -la models/asr/
ls -la models/llm/

# Убедитесь, что модели TTS скачаны
cd models/tts
ls -lh *.onnx
```

### Микрофон не работает

1. Убедитесь, что браузер имеет доступ к микрофону
2. Используйте Chrome/Firefox (не Safari)
3. Проверьте настройки микрофона в системе

### Медленная работа

1. Используйте меньшие модели:
   - ASR: `base` вместо `large-v3`
   - TTS: `low` вместо `high`
2. Увеличьте ресурсы Docker (RAM/CPU)

## 🔧 API Endpoints

### Backend (port 8000)
- `POST /start_session` - Создать новую сессию
- `POST /dialogue_file` - Обработать аудио файл
- `POST /end_session` - Завершить сессию
- `GET /scenarios` - Список сценариев

### ASR (port 9001)
- `POST /transcribe` - Распознать речь

### LLM (port 9002)
- `POST /ask` - Получить ответ от AI

### TTS (port 9003)
- `POST /speak` - Синтезировать речь
- `GET /voices` - Список доступных голосов

## 📁 Структура проекта

```
golosai-de/
├── asr/                    # Сервис распознавания речи
│   ├── Dockerfile
│   ├── main.py
│   └── requirements.txt
├── backend/                # Основной backend API
│   ├── Dockerfile
│   ├── main.py
│   └── requirements.txt
├── frontend/               # Web интерфейс
│   ├── Dockerfile
│   ├── main.py
│   └── requirements.txt
├── llm/                    # Сервис языковой модели
│   ├── Dockerfile
│   ├── main.py
│   └── requirements.txt
├── tts/                    # Сервис синтеза речи
│   ├── Dockerfile
│   ├── main.py
│   └── requirements.txt
├── config/                 # Конфигурационные файлы
│   ├── config.yaml
│   └── scenarios.yaml
├── models/                 # Директория для моделей
│   ├── asr/
│   ├── llm/
│   └── tts/
├── docker-compose.yml      # Конфигурация Docker Compose
└── README.md
```

## 🛠️ Разработка

### Локальная разработка отдельного сервиса

```bash
# Например, для frontend
cd frontend
pip install -r requirements.txt
streamlit run main.py
```

### Остановка сервисов

```bash
# Остановить все контейнеры
docker-compose down

# Остановить и удалить volumes
docker-compose down -v
```

### Просмотр логов конкретного сервиса

```bash
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f asr
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 👥 Авторы

- Arty Smoker - https://github.com/ArtySmoker

## 🙏 Благодарности

- [Whisper](https://github.com/openai/whisper) - ASR модель
- [Piper](https://github.com/rhasspy/piper) - TTS синтез
- [Streamlit](https://streamlit.io/) - Web фреймворк
- [FastAPI](https://fastapi.tiangolo.com/) - API фреймворк

## 📧 Контакты

Если у вас есть вопросы или предложения, создайте Issue в репозитории или напишите на email: your.email@example.com

---

⭐ Если проект был полезен, поставьте звезду на GitHub!