# ⚡ Быстрый старт GolosAI-DE

## 🚀 Установка за 3 шага

### 1️⃣ Клонирование и подготовка
```bash
git clone https://github.com/YOUR_USERNAME/golosai-de.git
cd golosai-de
chmod +x setup.sh
```

### 2️⃣ Автоматическая установка
```bash
./setup.sh
```

### 3️⃣ Откройте в браузере
```
http://localhost:3000
```

---

## 📝 Ручная установка

### Шаг 1: Создание директорий
```bash
mkdir -p models/{asr,llm,tts}
```

### Шаг 2: Скачивание моделей
```bash
./download_models.sh
```

### Шаг 3: Запуск
```bash
docker-compose up --build -d
```

---

## 🎮 Основные команды

### Управление сервисами
```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Статус
docker-compose ps

# Логи всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f frontend
```

### Пересборка после изменений
```bash
# Пересборка всех сервисов
docker-compose build

# Пересборка конкретного сервиса
docker-compose build frontend

# Запуск с пересборкой
docker-compose up --build -d
```

### Очистка
```bash
# Остановка и удаление контейнеров
docker-compose down

# + удаление volumes
docker-compose down -v

# Полная очистка Docker
docker system prune -a
```

---

## 🔧 Быстрые решения проблем

### Порт уже занят
```bash
# Измените порты в docker-compose.yml
ports:
  - "3001:3000"  # вместо 3000:3000
```

### Недостаточно памяти
```bash
# Измените модель ASR на меньшую в config/config.yaml
asr:
  model: "base"  # вместо large-v3
```

### Сервис не запускается
```bash
# Просмотр логов
docker-compose logs <service_name>

# Пересоздание контейнера
docker-compose up -d --force-recreate <service_name>
```

---

## 📊 Проверка работоспособности

### Проверка всех сервисов
```bash
# Должны быть запущены все 5 сервисов
docker-compose ps

# Проверка здоровья
curl http://localhost:8000/docs  # Backend API
curl http://localhost:9001/docs  # ASR
curl http://localhost:9002/docs  # LLM
curl http://localhost:9003/docs  # TTS
curl http://localhost:3000       # Frontend
```

---

## 🎯 Первое использование

1. **Откройте** http://localhost:3000
2. **Выберите сценарий** (Ресторан, Знакомство, Аэропорт)
3. **Нажмите на кнопку записи** 🎤
4. **Говорите** на немецком языке
5. **Остановите** запись
6. **Нажмите** "Отправить запись"
7. **Получите** ответ от AI с озвучкой

---

## 📚 Дополнительная информация

| Документ | Описание |
|----------|----------|
| `README.md` | Полная документация |
| `DEPLOYMENT.md` | Деплой на GitHub |
| `CONTRIBUTING.md` | Как участвовать в разработке |

---

## 🆘 Помощь

Возникли проблемы? 
1. Проверьте [README.md](README.md) → раздел "Решение проблем"
2. Просмотрите Issues на GitHub
3. Создайте новый Issue с описанием проблемы

---

## 🎉 Готово к работе!

Приятного использования GolosAI-DE! 🚀
