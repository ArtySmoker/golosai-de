# Руководство по внесению вклада в GolosAI-DE

Спасибо за интерес к проекту! Мы рады любому вкладу.

## 📋 Как внести вклад

### 1. Сообщение об ошибках

Если вы нашли ошибку:

1. Проверьте, не создан ли уже Issue с этой проблемой
2. Создайте новый Issue с подробным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Логи (если есть)
   - Версия Docker и Docker Compose

### 2. Предложение новых функций

1. Создайте Issue с описанием функции
2. Опишите use case и зачем это нужно
3. Дождитесь обсуждения перед началом работы

### 3. Pull Requests

#### Подготовка

```bash
# Форкните репозиторий на GitHub
# Клонируйте ваш форк
git clone https://github.com/YOUR_USERNAME/golosai-de.git
cd golosai-de

# Добавьте upstream remote
git remote add upstream https://github.com/ORIGINAL_OWNER/golosai-de.git

# Создайте ветку для работы
git checkout -b feature/your-feature-name
```

#### Разработка

1. Внесите изменения
2. Убедитесь, что код работает:
   ```bash
   docker-compose build
   docker-compose up -d
   # Протестируйте функционал
   ```

3. Зафиксируйте изменения:
   ```bash
   git add .
   git commit -m "feat: добавлена новая функция X"
   ```

#### Стиль коммитов

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

Примеры:
```
feat: добавлена поддержка английского языка
fix: исправлена ошибка распознавания длинных фраз
docs: обновлен README с новыми примерами
```

#### Отправка PR

```bash
# Обновите вашу ветку с upstream
git fetch upstream
git rebase upstream/main

# Отправьте в ваш форк
git push origin feature/your-feature-name
```

Затем создайте Pull Request на GitHub.

## 🏗️ Структура кода

### Backend (FastAPI)
- Используйте async/await где возможно
- Добавляйте type hints
- Обрабатывайте ошибки с HTTPException

### Frontend (Streamlit)
- Используйте session_state для состояния
- Добавляйте spinner для длительных операций
- Обрабатывайте ошибки с user-friendly сообщениями

### Docker
- Минимизируйте размер образов
- Используйте multi-stage builds где возможно
- Добавляйте healthchecks

## 🧪 Тестирование

Перед отправкой PR протестируйте:

1. Запуск всех сервисов:
   ```bash
   docker-compose up --build
   ```

2. Базовый функционал:
   - Запись с микрофона
   - Загрузка файла
   - Смена сценариев
   - Экспорт стенограммы

3. Проверка логов на ошибки:
   ```bash
   docker-compose logs
   ```

## 📝 Документация

При добавлении новых функций обновите:

- README.md - основная документация
- config/scenarios.yaml - если добавили сценарии
- API endpoints - если изменили API

## 🎨 Стиль кода

### Python
- Следуйте PEP 8
- Используйте Black для форматирования (опционально)
- Максимальная длина строки: 100 символов

### Именование
- Функции и переменные: `snake_case`
- Классы: `PascalCase`
- Константы: `UPPER_SNAKE_CASE`

## ❓ Вопросы?

Если у вас есть вопросы:
1. Проверьте существующие Issues
2. Создайте новый Issue с вопросом
3. Или напишите в Discussions

Спасибо за ваш вклад! 🙏
